{
  "name": "creatorshq-monorepo-relay",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "install": "cd frontend && npm install && cd ../backend && npm install",
    "start": "cd backend && npm start"
  }
}
